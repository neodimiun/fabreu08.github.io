React + TypeScript + Vite + Tailwind source for joseqc.com.

```bash
npm install
npm run dev
npm run build:pages
```

`build:pages` copies the production bundle to the repo root so GitHub Pages can serve it.
